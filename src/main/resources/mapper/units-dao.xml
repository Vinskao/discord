<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.mli.assetmybatis.dao.UnitsDAO">

    <resultMap id="unitsResultMap" type="com.mli.assetmybatis.model.Units">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="creationDate" column="creation_date" typeHandler="com.mli.assetmybatis.util.LocalDateTimeHandler"/>
        <result property="closedDate" column="closed_date" typeHandler="com.mli.assetmybatis.util.LocalDateTimeHandler"/>
    </resultMap>

    <select id="findAll" resultMap="unitsResultMap">
        SELECT * FROM units
    </select>

    <select id="findById" resultMap="unitsResultMap" parameterType="int">
        SELECT * FROM units WHERE id = #{id}
    </select>

</mapper>

